name: SPACEWARM CI

on:
  push:
    branches: [ main, master ]
  pull_request:
    branches: [ main, master ]

jobs:
  build-and-test:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout repository
      uses: actions/checkout@v4

    - name: Set up Go
      uses: actions/setup-go@v5
      with:
        go-version: '1.22'

    - name: Verify dependencies
      run: |
        go mod tidy
        go mod verify

    - name: Run tests
      run: |
        go test -v ./...

    - name: Build server binary
      run: |
        go build -v -o bin/spacewarm-server cmd/server/main.go
